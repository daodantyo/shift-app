# 複数店舗対応の切り替え手順

このアプリは「お店ごとにメールでログインし、データを分けて使う」形になっています。
**上から順に**行ってください(順番を変えると、今のお店の画面が一時的に使えなくなります)。

## 1. 先に、プレビュー版で自分のお店を登録してデータを引き継ぐ

本番(main)に入れる前に、ブランチのプレビューURL(Vercel にログインしていれば開けます)で準備を済ませます。
プレビューも本番も同じデータベースを見ているので、ここでやったことはそのまま本番でも有効です。

1. プレビューURLを開き、「はじめてのお店の方(新規登録)」からメールで登録 → 届いた確認メールのリンクを押す →「確認しました」
2. ログイン後、画面上部に出る **「📦 以前のデータを引き継ぐ」** を押す
   - シフト・売上・経費・設定・希望シフト・LINE対応表・抽選記録が、このお店にコピーされます
   - 今までキャストに配っていたURL(`?request=1` など、`shop=` なし)は、自動でこのお店につながるようになります
   - 今のLINE(LIFF)はそのまま使えます。LINEのトークンも Vercel の設定がそのまま使われるので、入力不要です
3. 「キャスト」タブ →「🏪 お店の設定」を開き、お店の名前を入れて保存(任意)

## 2. 本番に反映する(main にマージ)

GitHub で `feature/multi-shop` を main にマージします。Vercel が自動でデプロイします。
反映後、本番URLを開くとログイン画面になるので、手順1で登録したメールでログインしてください。

## 3. Firebase のルール(データの守り方)を更新する

`database.rules.json` の内容を、Firebase コンソール → Realtime Database →「ルール」に貼り付けて「公開」します。

- `shops/{お店ID}` は、そのお店でログインした人だけが読み書きできます
- キャスト向けページが使う `cast` / `shifts` / `settings` / `castLine` / `lottery` / `shiftRequests` だけは、ログインなしでも使えます
- 昔の場所のデータ(`shiftapp` など)は、ログインした人だけが読めます(引き継ぎ用に残してあります)
- `commissionApp` は今までどおり(別アプリ用)

**注意:** 手順2より前にルールを入れると、今の本番(古い版)が動かなくなります。必ず手順2のあとに。
**注意:** このルールを入れるまでは「お店の設定」にLINEトークンを登録しないでください(誰でも読める状態のため)。

## 4. Vercel の環境変数を追加する(他のお店がLINE送信を使うために必要)

1. Firebase コンソール → プロジェクトの設定 →「サービス アカウント」→「新しい秘密鍵の生成」で JSON をダウンロード
2. Vercel → shift-app → Settings → Environment Variables に追加
   - `FIREBASE_SERVICE_ACCOUNT` = ダウンロードした JSON の中身をそのまま貼り付け
3. 保存後、Vercel で「Redeploy」

※ これを入れなくても、最初のお店(環境変数 `LINE_TOKEN` / `ADMIN_LINE_ID` のお店)は今までどおり動きます。

## 5. 他のお店を招待する

1. お店の人に管理画面のURLを教え、「新規登録」してもらう
2. ログイン後、「キャスト」タブ →「🏪 お店の設定」で以下を登録してもらう
   - お店の名前
   - LINE Developers で作った **LIFF ID**(LIFFアプリのエンドポイントURLには、同じ画面に出る「希望シフト提出」のURLを登録)
   - LINE公式アカウントの **チャネルアクセストークン** と **管理者のLINEユーザーID**
3. 同じ画面の「キャストに案内するURL」をキャストに配ってもらう

## 手元で動かすとき(開発用)

`.env.local` に `REACT_APP_SKIP_VERIFY=1` を入れると、メール確認を飛ばしてログインできます(本番ビルドには影響しません)。
